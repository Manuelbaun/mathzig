# Audit 2026-07-10 — status + defect findings (reference for stream C)

External review of specs 01–17 against the repo at this date. Specs 12–20
cite this file as `[AUDIT]`. Line numbers valid at audit time — re-locate.

## Gate reality (protocol order)

| Gate | Result |
|------|--------|
| `zig build vm-baseline` | PASS 4/4 |
| `zig build test` | PASS 184/184 |
| `bun test` | **FAIL** — 880 pass, 15 fail, 1 skip, 25 todo / 921 |
| parity quick/full | not run (blocked by red bun gate) |

The 15 bun failures: 2 stale generated-gauntlet/idempotence
(`tests/ts/gauntlet_fuzz.test.ts:28-49`), 1 large-expression OutOfMemory
(`tests/ts/parity/integration.test.ts`), 12 translated MathJS core cases
(`tests/ts/parity/mathjs_examples/run_translated.test.ts:28-42` — decimal
rounding, matrix pow, unit magnitude, slicing/assignment, object mutation,
unit vectors).

## Verdict

No task 01–11 fully meets its literal acceptance contract ("all gates
green" is false). Strong subcomponents (narrow claims OK): B2 TS
graph/value transport, B3 setParam/reload, B4 scalar tick optimization,
B5 DSL lowering + VM-native scalar/matrix evaluator, A6 static unit
folding/result annotation + hybrid series metadata.

## Source-level defects (owned by task-18 unless noted)

- D1 `src/ts/aot_env.ts:70-94` — host writes use a private end-of-memory
  bump allocator (`hostHeapPtr`) instead of `instance.exports.alloc`; two
  allocation domains can overlap. ODE result writes included.
- D2 `src/wasm/compiler.zig:6197-6200,6433-6437` — AOT slice assignment is
  step-1-only; VM applies arbitrary positive steps
  (`src/vm/vm.zig:3895-3944`). Negative point indices also diverge.
- D3 `src/wasm/compiler.zig:3015-3019,5623-5762` — null and record-miss
  paths still emit NaN sentinels; task-04's "NaN sentinels eliminated"
  claim is false as stated.
- D4 `src/graph/schema.zig:365-371` — native inline manifest replaced with
  `"{}"` before parsing; validation ineffective.
- D5 `src/graph/runner.zig:238-242` — native runner never loads/executes
  .wasm; component is a VM-native graph evaluator, mislabeled.
- D6 `src/vm/vm.zig:1069-1088,1196-1204` — unit-bearing matrix literals:
  ordinary `mat_create` silently strips units, fused `mat_create_3`
  rejects them — optimizer-dependent semantics (owned by task-15).
- D7 `tests/parity/cases/matrix_units.json:2-72`,
  `units_aot.json:79-103` — expected values bless the silent strip
  (owned by task-15).

## Proof-obligation gaps (owned by task-19)

- P1 `tests/parity/generate_gauntlet.ts:729-805` — name-level coverage
  only; missing non-square boundaries, nested predicates, intermediate
  variadic arities; coverage assertion is count>0, not dimensions.
- P2 `tests/parity/fuzz.ts:486-490,589-594` — any two backend errors count
  as a semantic pass.
- P3 task-05 spec:38-40 — per-body standalone native-vs-wasm edge sweeps
  missing; standalone report: 267 covered / 266 skipped / 1 failed.
- P4 task-07 — record/string/complex host-written eval inputs via exported
  alloc not demonstrated.
- P5 task-10 — runBatch equivalence missing for matrix/complex/series;
  shared-memory rejected without the matrix-heavy experiment
  (`results.md:46-56` conclusion unsupported).
- P6 `tests/parity/cli.ts:450-493` + `package.json:7-16` — standalone
  backend advertised but excluded from the standard full gate.

## Documentation staleness (owned by task-20)

- `specs/README.md` — "landed"/"plan complete" while gate red and
  checkboxes unticked (partially corrected 2026-07-10).
- `docs/plans/wasm_node_graph/plan.md:19,29-63` — says no graph exists,
  A1 next, B5 deferred.
- `docs/plans/00_parity.md:24-32,84,164-188` — stale counts (43 files/98
  cases vs actual 53 files/534 backend cases); slicing gap description
  obsolete.
- `docs/plans/04_wasm_aot_parity.md:9-25` — machine-specific `file:///`
  links, stale roadmap refs.
- `package.json:31` — `test:parity:coverage` only greps a migration doc
  for TODO.

## Spec-refresh corrections applied to C-specs (2026-07-10)

- C1: baseline is 15 fails + 25 todos (not "~31"); gauntlet staleness must
  be FIXED not quarantined; stable identities + status taxonomy defined.
- C2: public port semantics = real manifest names; x/y/z is internal TS
  lowering; TS supports 3 aliases vs native 16; three execution modes must
  be distinguished; error-form goldens have no composed expression.
- C3: DSL input is JS strings (not bytes); TS has no graph-JSON byte
  loader; native `value_transfer.zig` does not parse hostile wire headers
  (real boundary = TS AotHostEnv + any Zig wasm-memory decoder);
  `src/graph/manifest.zig:128-153` conflates malformed wasm with absent
  section; sync elapsed-time asserts can't stop hangs.
- C4: hard-error membership must derive from actual resolvability (the
  `RequiredRuntimeSet` predicate), not `StandaloneTier`; dynamic-unit work
  narrows to honest detection (`unit_runtime` field) — real runtime unit
  ABI is a split task; `src/ts/aot_env.ts:763-777` target≈1 identity
  workaround documented.
