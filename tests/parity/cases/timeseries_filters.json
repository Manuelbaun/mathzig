[
  {
    "id": "ts_dropna_01",
    "expr": "len(dropna(series([0, 1, 2], [10, nan, 30])))",
    "vars": {},
    "expected": { "tag": "number" },
    "tolerance": 0,
    "skip": ["ts_wasm_vm"]
  },
  {
    "id": "ts_fillna_01",
    "expr": "last(head(fillna(series([0, 1, 2], [10, nan, 30]), 0), 2))",
    "vars": {},
    "expected": { "tag": "number" },
    "tolerance": 0,
    "skip": ["ts_wasm_vm"]
  },
  {
    "id": "ts_where_mean_01",
    "expr": "mean(series([1, 2, 3, 4], [10, -5, 20, -3])) where value > 0",
    "vars": {},
    "expected": { "tag": "number" },
    "tolerance": 1e-12,
    "skip": ["ts_wasm_vm"]
  },
  {
    "id": "ts_where_sum_and_01",
    "expr": "sum(series([1, 2, 3, 4, 5], [2, -1, 4, -3, 6])) where value > 0 and timestamp > 2",
    "vars": {},
    "expected": { "tag": "number" },
    "tolerance": 1e-12,
    "skip": ["ts_wasm_vm"]
  },
  {
    "id": "ts_where_mean_or_01",
    "expr": "mean(series([0, 1, 2, 3], [5, -10, 8, -2])) where value < 0 or timestamp >= 2",
    "vars": {},
    "expected": { "tag": "number" },
    "tolerance": 1e-12,
    "skip": ["ts_wasm_vm"]
  }
]
