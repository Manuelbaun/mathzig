[
  {
    "id": "chained_nested_record_access",
    "expr": "{ a: { b: { c: 42 } } }.a.b.c",
    "vars": {},
    "expected": { "tag": "number" },
    "tolerance": 0,
    "note": "Deeply nested record member access"
  },
  {
    "id": "chained_index_on_result_matrix",
    "expr": "size([1, 2, 3]).cols",
    "vars": {},
    "expected": { "tag": "number" },
    "tolerance": 0,
    "note": "Member access on result of a function returning a record/matrix"
  },
  {
    "id": "chained_complex_matrix",
    "expr": "[1 + 2i, 3 - 4i; i, -i] * [1; 1]",
    "vars": {},
    "expected": { "tag": "matrix", "shape": [2, 1] },
    "tolerance": 1e-12,
    "skip": ["ts_wasm_vm"],
    "note": "Arithmetic on complex matrices"
  },
  {
    "id": "chained_unit_matrix",
    "expr": "[1m, 2m; 3m, 4m] * 2",
    "vars": {},
    "expected": { "tag": "matrix", "shape": [2, 2] },
    "tolerance": 1e-12,
    "skip": ["ts_wasm_vm"],
    "note": "Scalar multiplication on a matrix containing unit values"
  },
  {
    "id": "chained_indexing_expression",
    "expr": "m = [10, 20; 30, 40]; m[1, 1]",
    "vars": {},
    "expected": { "tag": "number" },
    "tolerance": 0,
    "note": "Indexing with variables and assignment"
  },
  {
    "id": "chained_slice_on_result",
    "expr": "range(1, 10)[1:3]",
    "vars": {},
    "expected": { "tag": "matrix", "shape": [1, 3] },
    "tolerance": 0,
    "note": "Slicing the result of a generator function using 1D vector indexing"
  }
]
